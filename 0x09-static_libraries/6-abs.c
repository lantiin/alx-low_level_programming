#include "main.h"
/**
  *_abs - calculates the absolute  value of an integer
  *@n: the integer to be calculated
  *Return: always 0
  */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

