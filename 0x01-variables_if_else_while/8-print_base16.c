#include <stdio.h>
/**
  *main - Entry
  *Return:on success 0
 */
int main(void)
{
	int n, m;

	for (n = '0'; n <= '9'; n++)
		putchar(n);
	for (m = 97; m <= 102; m++)
		putchar(m);
	putchar('\n');
		return (0);
}
